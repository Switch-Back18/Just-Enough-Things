package fr.mathisskate.justenoughthings.screen;

/*import com.mojang.blaze3d.matrix.MatrixStack;
import com.mojang.blaze3d.systems.RenderSystem;
import fr.mathisskate.justenoughthings.container.ItemCollectorContainer;
import fr.mathisskate.justenoughthings.util.References;
import net.minecraft.client.gui.screen.inventory.ContainerScreen;
import net.minecraft.entity.player.PlayerInventory;
import net.minecraft.util.ResourceLocation;
import net.minecraft.util.text.ITextComponent;

public class ItemCollectorScreen extends ContainerScreen<ItemCollectorContainer> {
    private final ResourceLocation GUI = new ResourceLocation(References.MODID,
            "textures/gui/item_collector_gui.png");
    public ItemCollectorScreen(ItemCollectorContainer screenContainer, PlayerInventory inv, ITextComponent titleIn) {
        super(screenContainer, inv, titleIn);
    }

    @Override
    protected void drawGuiContainerBackgroundLayer(MatrixStack matrixStack, float partialTicks, int x, int y) {
        RenderSystem.color4f(1f, 1f, 1f, 1f);
        minecraft.getTextureManager().bindTexture(GUI);
        int left = guiLeft;
        int top = guiTop;
        blit(matrixStack, left, top, 0 , 0, xSize, ySize);
    }
}*/
